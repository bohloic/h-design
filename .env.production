# ✅ FIX #4 : Le commentaire était inline ce qui corrompait la valeur de VITE_API_URL.
# En production, mettre l'URL complète du backend (ngrok, VPS, etc.)
# En prod locale, laisser vide (le proxy Vite ne s'applique pas au build)
VITE_API_URL=https://rosaline-ceroplastic-humblingly.ngrok-free.dev